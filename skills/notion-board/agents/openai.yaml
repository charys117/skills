interface:
  display_name: "Notion Board"
  short_description: "Use Notion as the repo interface and keep board data in sync."
  default_prompt: "Use $notion-board to create, sync, or inspect the Notion board for this repository."
policy:
  allow_implicit_invocation: true
